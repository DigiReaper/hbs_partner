import 'package:flutter/material.dart';

class AcceptedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Accepted'),
      ),
      body: Center(
        child: Text('This is the Accepted page'),
      ),
    );
  }
}